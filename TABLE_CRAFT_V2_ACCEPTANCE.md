# 台内精修 V2 · 本地验收

2026-09-08。状态：Blender 建模、游戏接入、完整检查与真实入口验收完成；本地未提交、未推送、未同步妙搭、未发布。延续用户认可的球房 V1，Git 基线仍为唯一主树 `main@741cae41e7686d989460a2f3feb44de677ea580d` / v1.5.1。

## 最终行为

- 台呢改为 160mm 无缝细织纹，以米制 UV 覆盖整张台面；修正原来的负坐标 UV 被 ClampToEdge 拉成边缘纹理的问题。512² 颜色/法线/粗糙度三图共享 mipmap，近景可见细纹，远景自然退去。
- 库边按实际包呢剖面展开同尺度织纹；鼻尖和内凹面跨短圆弧接头连续法线，顶面保留原实体三角法线。原有顶点、三角形、鼻尖高度和物理碰撞线保持一致。
- 六袋保留共享几何的闭合皮壳、1.5mm 皮裙和真实网袋；新增 Blender 制作的 520 段双道针脚、细圆包边。皮革有独立 UV、细颗粒和克制的抛光，袋腔更深。所有新增顶点均由原皮壳顶面承托，不侵占开放球路。
- 球贴图提升至 1024×512，六点母球帮助观察真实角速度；调整树脂粗糙度及清漆反光。新增 16 球共享的单批接触阴影，读取最终显示球位；规划预览同步，落袋隐藏，卸载释放实例缓冲。没有速度拖尾、伪造滚动或新的动画时钟。
- 继续使用现有 240Hz 物理及角速度积分。摩擦系数、球半径、库边/球碰响应、袋口捕获、规则、AI 和输入未变；本轮没有新增真实三维袋壁或绳网刚体模拟。

## Blender 与资产

通过用户配置的 Blender MCP 在 Blender 5.2.1 LTS 制作独立 `BJ8_Table_Craft_V2` 场景。`assets/blender/table-craft-v2/table-craft-v2.blend` 包含可编辑针脚和包边，以及当前游戏的 167 个台面/库边/球体参照网格、实际法线与 UV、17 张打包原创贴图。球房 V1 和用户原有场景保留。参考集合不进入运行时 GLB。

| 产物 | 实际结果 |
| --- | --- |
| `src/scene/assets/table-craft-v2.glb` | 492,004 bytes；2 网格、25,744 三角形；无外部纹理/解码器 |
| GLB SHA-256 | `8ad4e94a876ed471c1c7384146e8944bbdd91a9a350daed07fdc4fdef44f5864` |
| 制作工程 | 2,986,088 bytes；文件及纹理哈希见同目录 `provenance.json` |
| 最终 `dist/index.html` | 2,892,342 bytes，约 2.76 MiB；继续通过 10 MiB 单 HTML 门禁 |
| HTML SHA-256 | `56e1bb5782c50d75ed14bea32129c1f781f5cb68fa566b86498b22f44a499cea` |
| 源码与构建输入指纹 | `528acfa15d36683198d2559c7553bf01bf0f8e4f2166032b5c15dd22516436a9`；见 `shots/table-craft-v2/source-snapshot.json` |

制作脚本为 `scripts/build-blender-table-craft.py` 与 `scripts/update-blender-table-preview.py`。完整用户原文、尺寸、来源和哈希保存在 provenance；全部本地原创，无付费生成或第三方素材。初次导出发现包边端部少量悬出和面数超预算，已回缩端部 1mm 并简化针脚细分；最终没有放宽承托容差或资产预算。

## 验证证据

- 开始时 `npm ls --depth=0` 成功，没有安装依赖或修改锁文件。
- 定向几何/落袋检查 38/38：包括六项新增资产与 UV/法线测试、原皮壳承托/闭合、库边斜接、落袋轨迹。GLB 验证实际世界坐标顶点、法线、三角形、当前合同哈希、六袋锚点，以及全部细节顶点到原皮壳的垂直承托距离小于 1mm。
- 唯一完整 `npm run check` 通过：48 个测试文件、358 项测试、类型检查及生产构建。日志 `shots/table-craft-v2/check.log`。Vite CJS 与单包大小提示仍存在，构建无错误。
- `node scripts/verify-table-craft.mjs`：19/19，覆盖桌面/手机真实开始对局、模型及 UV 装配、静止停帧、实际视角控件、鼠标出杆与真实角速度旋转、绘制时球/接触阴影位置同步、落袋隐藏、失败回退和卸载竞态。
- 最后补充 `LIFECYCLE_ONLY=1`：2/2，确认实例缓冲释放事件和晚到资产回收。接触阴影同步在实际 renderer 绘制边界采样，避免把物理更新与展示帧之间的中间状态当作画面错位。
- 既有六袋/四机位检查 8/8；既有温控门禁 10/10。日志 `pockets.log`、`thermal.log`。
- 最终 `PRODUCTION=1 node scripts/verify-table-craft.mjs`：10/10。真实入口 `http://127.0.0.1:5199/`，主树 Vite preview；浏览器收到的 HTML SHA-256 与最终构建一致，两视口均无外部 GLB 请求、JS 错误或横向溢出。

上述浏览器为本机无头 Google Chrome、干净测试页、无需登录。桌面 1280×800，手机视口 390×844 / 2×；温控测试另覆盖 3× 设备像素比下的 2× 绘制上限。模型、浏览器和构建重活串行执行。最终保留本任务 preview 服务供用户试玩，其余任务浏览器和开发服务已关闭。

## 视觉与限制

- `production-desktop-low.png`、`production-mobile-low.png`：最终构建通过实际控件进入低机位的游戏画面。
- `pocket-side-macro.png`、`pocket-corner-macro.png`：明确使用开发检视机位的袋口微距截图；其后运行时唯一修改为卸载时释放接触阴影缓冲，不影响画面。
- `blender-viewport.png`：实际 Blender 制作视口；灯光与程序化材质预览不等同于游戏渲染。

这轮增加细节，未声称性能变快。同样首局静止画面采样由 V1 的 272 calls / 107,723 triangles 变为 275 / 133,499；新增两个静态模型批次与一个接触阴影批次。球纹理分辨率提高也会增加纹理显存。既有按需渲染和温控门禁通过，但这些结果不是等热态性能基准，也不代表真实手机长时间发热验收。完整 AI 整局、音效、真机长时运行和正式线上入口未在本轮重新测试。
