# 默认左右微调 × 按需拨轮精瞄 · 验收单

> 包版本：`1.4.0`
> 状态：本地人工验收通过，作为 `v1.4.0` 基线，未发布
> 验收重点：本地 main 视觉基线、默认左右微调、灯泡三入口、拨轮显式开启、世界杆向一致

## 操作规格

1. **开球摆放**：开始对局后实体母球自动位于开球线中点并直接进入瞄准；按住母球可在合法开球区拖放，越界或与目标球重叠时保持最后一个合法位置。
2. **默认左右微调**：开局就绪后，桌面与手机竖屏都显示球杆左右键；单击固定移动 0.004rad，长按 320ms 后以 140ms 间隔低速连续移动。第一人称与俯视都修改唯一世界杆向，拨轮默认不出现。
3. **360° 粗瞄**：抓住球桌瞄准线约 400px 转完整一周；拨轮采用相对增量，可反复抬手续拨，没有左右行程边界。
4. **灯泡第三入口**：灯泡展开“瞄准辅助线 / 走位与击球复盘 / 拨轮瞄准”；开启拨轮后原位替换左右键，轻点拨轮才进入/退出精瞄。
5. **固定精瞄**：精瞄传动恒定为粗档的 1/8，和目标球、袋口、辅助线开关无关；方向键沿当前控件轴向微调。
6. **压感叠加**：支持可靠压力的触控笔/屏幕可在用户当前档位上进一步收紧；普通触摸固定 0.5 与鼠标严格回退原档速度。
7. **几何隔离**：目标球预测线、白球分离线与规划仍共享 physics 的恢复系数与 throw 模型，但玩家拨轮切档不消费袋口解。

## 明早人工验收（约 5 分钟）

- [ ] 第一人称瞄准任意球，切俯视再切回：球杆仍指向同一条世界球路。
- [ ] 常规回合抓住瞄准线快速横扫：可连续跨过桌面四个方向，不被某个袋口卡住。
- [x] 点击开始对局：实体母球在开球线中点自动就绪并直接进入瞄准。
- [x] 按住母球拖动：只在合法开球区移动；越界或与目标球重叠时保持最后合法位置。
- [ ] 手机 390×844 竖屏：左右键完整可见，没有被媒体样式隐藏或裁出视口。
- [ ] 单击左/右键：每次只移动 0.004rad；按住后连续移动但不会触发控件拖位或快速旋转。
- [ ] 点击灯泡：三个独立入口完整显示；开启拨轮后左右键消失，关闭后恢复。
- [ ] 幽灵球落在非袋口方向：拨轮仍显示“方向”粗档。
- [ ] 把目标球延长线扫过任意袋口：拨轮保持粗档，速度不自动变化。
- [ ] 轻点拨轮后进入精瞄，再拨动时明显变慢；再次轻点回到粗档。
- [ ] 多次左右拨动并抬手续拨：可以跨越完整 360°，没有滑块端点。
- [ ] 用 30°–45° 切球按预测线出杆：目标球实际初始方向与线一致。
- [ ] 打完本组最后一颗后打开 💡 走位：若 8 号有可行路径，下一步能出现 8 号。
- [ ] 连续“再来一局”，在新局打到与上一局相同杆数：每一杆都能正常结算。

## 自动化证据

```bash
npm run check
# typecheck + 235/235 vitest + production build

BROWSER_URL=http://127.0.0.1:9333 GAME_URL=http://localhost:5199/ \
  node scripts/verify-interaction.mjs
# 41/41

BROWSER_URL=http://127.0.0.1:9333 GAME_URL=http://localhost:5199/ \
node scripts/verify-precision-aim.mjs
# 14/14

BROWSER_URL=http://127.0.0.1:9333 GAME_URL=http://localhost:5199/ \
  node scripts/verify-control-intent.mjs
# 6/6

BROWSER_URL=http://127.0.0.1:9333 GAME_URL=http://localhost:5199/ \
  node scripts/verify-mobile-portrait-v12.mjs
# 46/46

BROWSER_URL=http://127.0.0.1:9333 GAME_URL=http://localhost:5199/ \
  node scripts/verify-aim-accuracy.mjs
# 19/19
```

## 同批修复

- 新局重置 shot 结算守卫，避免跨局相同 shotId 被误判重复。
- 键盘瞄准改为按帧间隔积分，带加速度与最高速度，60/120Hz 等价。
- 球碰迭代改用明确碰撞事实，纯 z 方向逆序三球链不漏传播。
- planner 标定改参后重算概率；清组后继续规划 8 号。
- 新规划抢占旧 worker，旧 Promise 明确取消，不再悬挂或队头阻塞。
- 统一 physics `POCKETS`，移除 planner 袋口坐标镜像。
- 妙搭构建内联为单 HTML，避免签名对象存储重定向破坏模块入口。

[PROTOCOL]: 规格或验收结果变化时更新本文，然后检查 CLAUDE.md
