# opponent/

> L3 | 父级: `../CLAUDE.md`

## 成员清单

`model.ts`: 自适应对手纯领域模型——以现有 erf 线路容错概率归一化每杆表现，维护玩家执行、走位、犯规与置信度画像；把同一玩家画像映射为局间锁定的陪练/挑战 `OpponentProfile`，并提供容错本地持久化。

`model.test.ts`: 模型确定性回归，覆盖简单/困难球证据差异、辅助降权、每杆防抖上限、置信度冷启动、陪练/挑战映射与损坏存储降级。

[PROTOCOL]: 变更成员时更新本文件，然后检查 `../CLAUDE.md`
