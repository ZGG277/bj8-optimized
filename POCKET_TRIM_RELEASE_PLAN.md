# 袋口落地与发布合同

日期：2026-09-05。状态：T1–T4实现完成；完整机器检查及本地基础视觉通过，发布门禁未全部完成，未发布。

候选版本：v1.5.0（本地与远端尚无同名标签）。中央只同步 package.json 与锁文件根项目版本，不改变任何依赖项。

## 用户目标与授权

用户要求「派发任务，进行落地开发，验收无误后发布」。执行已研究的袋口方案，保留本地已验收的引导、相机和物理优化。发布目标为现有瓜瓜台球 `app_17b18dh5axj`，正式入口沿用 README，不新建应用、不改访问权限、不发通知或飞书文档。

用户已于2026-09-05明确授权主仓提交、带注释v1.5.0标签、GitHub推送，以及妙搭包装仓提交与推送；发布本身沿用上一条「验收无误后发布」授权。不扩大到权限变更、通知或飞书文档写入。

## T0 版本和来源

- 唯一源码树：`bj8-main-integration`，main，HEAD `0e1e7193971d0aeea78075c4d980058ea25e63e2`，GitHub origin/main 已只读核实同值。
- 实施起点源码：`f95ea7349ec417aecadbd40178d4897e1be42c330d3bc776138bb412bb4582eb`（149 文件）。起点 dist：1,286,748 bytes，SHA256 `0a9ea5aedc4aa8bb3ad27f2d180a5993751bd33ee9e423db15021627c92bdee1`。
- 工作树现有改动不是纯袋口补丁：包括本任务前轮分派的 onboarding/camera/physics/pocket 实现和回归，来源记录在 ONBOARDING_CAMERA、OPTIMIZATION、CONTROL_HOVER 验收文档与研究方案。
- 用户独立的 `IntroScreen.tsx` 和 `styles/layout.css` 改动保留且禁止本轮修改；各自 SHA256 为 `352dc8bd4ad1f5d7fd0ca16bc0829101be27c0ec7973b1ee4957fd98cbb1cb1c`、`69accbe0b39fc3eb980ba73d43e8181e47f232e98df7d98287f6714ffdac3953`。
- 妙搭包装树初始干净，`sprint/default` / `4f342a0`；只消费已验收主树产物，不反向修改产品。
- 两个工作树 `npm ls --depth=0` 成功，不安装依赖。原 5199 preview 保留，禁止重复启动服务。

## 分工与顺序

1. **Astra / xhigh，唯一几何写入者**：续接专项任务 `01a06f6a-6de1-7b03-804c-a21973638788`，执行研究方案 T1–T4；负责 Scene3D 与 pocket-render 及直接测试/地图。不得修改 UI、相机、物理、规则、依赖和包装，不提交推送发布。
2. **Spark 固定验收**：几何源码冻结且精确命令可执行后派发，绑定源码指纹；只运行既定 check/build 和证据采集，不修产品，不调整验收定义。失败回中央诊断，不重复同一失败命令。
3. **中央集成**：核对最终几何、保护文件与产物来源，准备稳定的本地真实入口，实际截图比较 D01–D06。未满足研究文档 §8 的必要门槛不得发布。
4. **边界审查员，只读**：在稳定发布候选上检查脏树来源、纳入文件、权限、版本与包装同步契约；不重复运行固定测试。
5. **中央发布**：在 Git 动作得到明确授权、所有前置验收通过后创建版本标签，同步已验收单 HTML 到包装；构建/推送指定分支/创建 release/查询状态；核对线上产物指纹。
6. **体验审查员，只读源码**：部署后的最终正式入口，桌面1280×800/手机390×844和关键旅程，记录实际 iframe、资源、构建、截图与结果。只在可恢复独立测试球局操作；不修改权限或发布。

GPU throttle 存在，以上任务串行交接。实现者明确停止写入后才冻结、验收或移交。测试通过不等于视觉通过，release finished 不等于线上验收通过。

发布准备补充：妙搭 sprint/default 远端与本地4f342a0一致；上次 release 7680324935008898031 也绑定该提交。远端 main 已有平台生成变化，本轮不擅自合并或改用 main 发布。官方 CLI 已刷新同一应用失效的 Git 凭证，没有降级钥匙串。主仓 post-commit 有 Graphify 后台索引；发布候选提交使用该索引自身支持的 GRAPHIFY_SKIP_HOOK=1，避免源码冻结后后台改图或叠加负载，不关闭测试/安全钩子。旧图不包含当前 pocket-render 或包装同步节点，不作为本轮源码与发布事实依据。

公开产物边界：前轮 Spark 原始交互 JSONL 与 stderr 日志仅留本地，`.gitignore` 精确排除两条路径，不删除文件。可公开的源码、专项计划、筛选后的测试结果和必要截图由发布前边界审查核对。

## 发布范围与停止条件

- 几何与数值标准沿用 `POCKET_TRIM_RESEARCH_PLAN.md` §8：最终网格无自交/倒折，后弧及端头真实接合，不改物理袋宽/捕获曲线，不封正常进球口；不能只测中间法向。
- 图像/浏览器标准：用户实际入口、两标准视口、六袋两端/后弧/斜视，D02/D04/D06重点对照；不能仅用远侧一图或临时相机藏缺陷。
- 发布必须证明源 commit/tag → dist SHA → 包装 BUILD_PROVENANCE → 线上实际 HTML 一致；仅使用已确认正式域名。
- 凭证或权限确实不足、用户未授权必要 Git 写、变更来源不明、视觉仍有缺漏、物理不变量变化，均停止对应发布步骤；保留本地产物，不清理或回退用户改动。
- 最终记录实施文件、机器与浏览器证据、commit/tag/release、可访问链接、剩余未验项；不得提前宣布发布成功。

## 当前接续点（2026-09-05）

- Astra已停止写入。最终sourceSHA `c0abce477a32c3638af818fc0c13b960b3a68737639ef64be0522c5612b4ea24` /151文件，两个保护文件不变。
- Spark固定验收因错误父目录执行而停止；中央接管正确主树的唯一一次完整check：46文件348测试、类型检查、生产构建全部通过。日志与截图见 `shots/pocket-trim-release-20260905/central-acceptance.md`。
- 新dist与5199实际响应逐字节一致：1282167 bytes，SHA `8418de1f32036cc89fdc684145e749989099425c57da107231e6aeabe116f602`。本地桌面俯视、低位斜视和手机全台/开球截图已核对。
- 边界审查仅完成两仓Git基线/包装干净状态核对，因审查者文档命令在错误父目录展开glob失败，返回NEEDS_EVIDENCE，未完成候选公开文件来源审查。不能声称边界门禁通过；也不能将其“dirty不合格”的描述理解为代码失败——授权后的已知改动提交是包装同步正常前置步骤。
- 后续用户已明确给出上述Git授权，原授权等待项关闭。
- 接续必须补完尚缺的候选文件来源审查与研究§8剩余视觉/运行门槛，再在获得Git授权后同步、发布并验证正式入口；不重复已通过且源码未变的348项测试。已关闭本任务浏览器，保留5199用户预览。

## 授权后发布前收口

边界审查员补完精确文件审查后返回PASS：五份来源记录、公开候选文本、两保护文件与source/dist指纹匹配，未发现常见凭据/私钥模式；原始对话日志仍留本地。

中央补充 `side-a.png`、`side-b.png` 及实际角度微调观察：六袋后弧/端头连续，未见角度变化导致缺面。D01–D06本次缺漏复现均关闭；特别D02/D04/D06近侧组合与上轮desktop-top/mobile-top及两侧斜视交叉核对，合法入球U口与网孔保留。完整证据位于 `shots/pocket-trim-release-20260905/`。

`visibility-matrix.cjs` 对原样Scene3D最终网格进行12方位×3高度×6袋×9个护口面采样，共1944条射线，missing=0；实际库边/木框遮挡另记，不把被遮挡面当成缺面。此检查与已通过的闭壳/绕序/实际面域承托测试共同验证几何，不替代真机长期性能测试。

本次发布接收上述几何修复、348项回归和实际桌面/手机基本旅程证据；不宣称长期等热态benchmark、真机高DPR、全复杂布局穷举通过。实际绘制采样未见增加，固定几何预算与回收路径保持。正式发布后另记录commit/tag/包装provenance/release/最终入口，不提前将此候选标为已上线。
