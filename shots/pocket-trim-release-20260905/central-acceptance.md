# 中央本地验收记录

2026-09-05。状态：本地实现与机器检查完成，发布尚未执行。Git 提交、标签、两仓推送的独立授权尚待用户明确答复。

## 输入与机器证据

- 主线 main / HEAD `0e1e7193971d0aeea78075c4d980058ea25e63e2`。
- 前后 sourceSHA 均为 `c0abce477a32c3638af818fc0c13b960b3a68737639ef64be0522c5612b4ea24`，151 文件；两个保护文件哈希保持实现交付文档中的值。
- Spark 任务 `01a06fe8-d1c9-70a2-aca2-7d3502ae77db` 在父目录误执行，Step 1 的 empty 依赖树不属于主树，因此不能用其“通过”作为产品证据；Step 2 找不到脚本后停止。原失败报告保留供追溯，中央未重派。
- 中央在正确主树执行 `npm ls --depth=0`、快照、`node --check scripts/verify-pockets.mjs`、`git diff --check` 均成功。
- 中央唯一一次 `npm run check` 退出0，46文件348测试通过，类型检查及生产构建成功；完整日志 `central-check.log`。
- 新 `dist/index.html` 1282167 bytes，SHA256 `8418de1f32036cc89fdc684145e749989099425c57da107231e6aeabe116f602`。
- `http://localhost:5199/` HTTP200，响应长度和SHA同上，Buffer逐字节对比dist为true；不是仅凭HTTP状态或旧缓存验收。

## 实际浏览器证据

本地应用无需登录。Codex in-app Chromium 独立标签4，桌面CSS1280×800、移动CSS390×844。没有修改产品运行时或清空用户存储，使用可丢弃的本地陪练局。

- 桌面开始陪练→俯视：六袋包边连续，原角袋后方露地板区未出现；默认白球、瞄准线、拨轮可见。截图 `desktop-top.png`。
- 开启手动视角→实际拖动旋转→键盘降低视角：所见近侧角袋/中袋皮壳及木框接合连续，未见旧折叠缺口。截图 `desktop-oblique.png` 为稳定后的低位斜视，不是全台构图证据。
- 手机重新载入→陪练→俯视：六袋及整台在视口范围内，包边连续。截图 `mobile-top.png`。
- 手机Enter开球→球组散开：自动保持全台，出杆提示可见。截图 `mobile-after-shot.png`。
- 页面错误日志未见error；保留既有 THREE.Clock 和 PCFSoftShadowMap 弃用warn。没有把这些警告当成新增故障。
- ego-browser旧基线截图曾超时，已关闭该空间；该失败不算视觉通过。最终视觉证据均由可用的in-app入口取得。

## 运行成本采样与限制

ego-browser临时页面级包装WebGL绘制API进行观测，没有产品改码。旧构建1280×800开始陪练/俯视时观测到318次绘制调用、132609三角；新构建同一操作首场景样本310次、122033三角，后续热控/按需帧117次、42235三角。采样只表明所测帧未增加成本，**不是长期平均FPS、等热态基准或温控门禁**，不得以此宣称移动硬件性能验证通过。

仍未验证：真实手机硬件/高DPR、全机位慢速旋转闪烁、幽灵球至目标袋的完整复杂布局回归、正式部署后的iframe/资源/线上产物指纹。物理本轮未改，348项包含既有物理回归；未声称经过真实球台实验标定。

原5199用户预览服务保留。自建ego空间16、17和in-app标签4已关闭，临时视口override已reset。没有提交、标签、推送、包装同步、release或对外通知。
