# 净息 · 实时联网对战方案设计（评估稿）

> 状态：评估稿，待 codex 审核，2026-07-29 决策
> 作者：Kimi（2026-07-28）
> 范围：只做方案设计与利弊分析，不含任何代码改动
> 关联文档：`README.md`（架构总览）、`POSITION_PLAY_DESIGN.md`（设计文档体例参照）

---

## 1. 背景与目标

「净息」当前为单机游戏（玩家 vs AI「顾燃」），发布于飞书妙搭。目标：支持**两名真人玩家联网对战**（PvP）。

候选实现路径两条：

- **方案 A**：借助 Kimi Websites（Kimi 建站，全栈生成 + 数据库 + 一键发布）实现，数据库轮询同步
- **方案 B**：保持现有静态托管，新增专用实时层（WebSocket 服务）同步

本文档拆解需求、设计两方案共用的同步协议、分别给出架构与数据设计，最后做对比与建议。

---

## 2. 现状架构资产盘点（对战相关）

以下结论均来自源码核实（2026-07-28），是协议设计的事实基础：

### 2.1 击球输入契约极小

物理内核击球入口（`src/physics.ts:257`）：

```
strikeCueBall(world, angle, power, spin?)
  - angle: 世界角（弧度）
  - power: 力度
  - spin.x: 高/低杆（+跟进 / -缩杆）
  - spin.y: 侧旋（塞）
```

**每杆需要同步的数据 = 4 个浮点数**。自由球摆放（`placing` 阶段）额外需要白球坐标 (x, z)。摆球需要 1 个随机种子（见 2.3）。

### 2.2 确定性物理 + 纯规则状态机

- 240 Hz 固定步长物理内核（`src/physics.ts`），主步进循环**无随机源**
- 规则层为纯状态机（`src/match/match-machine.ts`）：`ShotFacts → RoundResolution` 原子迁移，双方各自运行必然得出相同规则结论
- `MatchState.actor` 只有 `'player' | 'opponent'` 两个角色（`src/match/types.ts:8`）——PvP 中**每个客户端都以自己为 `player` 运行本地状态机**，天然同构，无需改造规则层视角

### 2.3 随机源清单与处置

| 位置 | 用途 | 对 PvP 的影响 | 处置 |
|---|---|---|---|
| `createInitialWorld(rng)`（`useGameState.ts:29,58` 传入 `Math.random`） | 摆球随机 | **有影响**：两端球堆初始布局必须一致 | rng 已可注入 → 每局由发起方生成 seed，两端用同一种子 PRNG |
| `planSimpleShot` 内 `Math.random`（`physics.ts:363`） | AI 击球执行误差 | **无影响**：PvP 无 AI；即使保留 AI 模式，误差在出杆前已并入 angle，网上传的是最终参数 | 无需处理 |
| `src/planner/`（蒙特卡洛搜索） | 💡 走位规划 | **无影响**：纯本地展示层，不进权威状态 | 无需处理 |
| `src/textures.ts` / `audio.ts` | 贴图/音效 | 无影响 | 无需处理 |

### 2.4 AI 对手是天然的替换点

`src/hooks/useOpponentAI.ts` 的编排模式：`match.phase === 'opponent'` 时自动调度，产出 `(angle, power)` 后调 `strikeCueBall`。PvP 中该 Hook 的角色由"本地 AI 决策"换成"等待远端击球事件后重放"，接口形状基本一致。

### 2.5 跨浏览器浮点确定性风险（重要）

ECMAScript 不保证 `Math.sin/cos/atan2` 等超越函数在不同引擎（V8 / JavaScriptCore / SpiderMonkey）结果逐位一致。两名玩家使用不同浏览器时，**纯输入锁步（lockstep）长跑会漂移**。

**结论：不采用帧级锁步。** 利用台球回合制特性，采用"**击球参数广播 + 停球快照权威**"混合模型（见第 4 节），从协议层面免疫浮点差异。

---

## 3. 联网对战需求拆解

| # | 需求 | 说明 | 同步频率 |
|---|---|---|---|
| R1 | 房间/邀请 | 创建房间得邀请码/链接，对手加入 | 一次性 |
| R2 | 身份 | 区分先后手、昵称；可选登录 | 一次性 |
| R3 | **击球同步（核心）** | 一杆的输入参数 + 停球结果送达对手 | 每杆 1 次 |
| R4 | 自由球摆放同步 | 犯规后白球摆放位置 | 每局 0–N 次 |
| R5 | 规则状态一致性 | 分组、犯规、胜负两端一致 | 由 R3 派生 |
| R6 | 断线重连 | 刷新/掉线后恢复对局 | 低频 |
| R7 | 瞄准过程观战（可选） | 实时看到对手杆向变化 | 高频（10–20 Hz） |
| R8 | 战绩/排行榜（可选） | 历史对局记录、胜率 | 每局 1 次写 |
| R9 | 表情/快捷聊天（可选） | 局内互动 | 低频 |

**关键观察**：核心闭环（R1–R6）全部是**低频事件**——台球一杆从瞄准到停球约 5–15 秒，每杆只需 1 次消息往返。R7 是唯一需要高频通道的需求，可作为二期。

---

## 4. 同步协议设计（两方案共用）

### 4.1 权威模型：快照权威 + 本地重放

不锁步、不逐帧传球位置。协议单元是"一杆"：

```
ShotRecord = {
  seq: number            // 全局递增序号，对局内唯一，用于排序/去重/断点续传
  matchId: string
  actor: 'host' | 'guest'
  input: {
    angle: number
    power: number
    spinX: number        // 高/低杆
    spinY: number        // 侧旋
    placement?: { x: number, z: number }   // 自由球摆放时存在
  }
  settled: {             // 出杆方模拟停球后的权威快照
    balls: Array<{ n: number, x: number, z: number, active: boolean }>  // 16 球
    facts: ShotFacts     // 首碰/进球/犯规等，供规则层校验
  }
  clientTime: number
}
```

流程：

1. **出杆方**：本地输入 → `strikeCueBall` → 240 Hz 模拟至停球 → 上传 `ShotRecord`（含快照）
2. **接收方**：收到 `ShotRecord` → 用 `input` 本地重放（两端画面体验一致，享受完整物理动画）→ 停球后**以 `settled` 快照为准强制校正**球位（吸收一切浮点/实现差异）→ 规则层用快照推导 `ShotFacts` 结算
3. 快照极小：16 球 × 4 字段 ≈ 200 字节（JSON 约 1 KB），每杆一次，带宽可忽略

**防作弊说明（MVP 阶段）**：信任出杆方快照（客户端权威）。熟人邀请制对战可接受；若未来做公开匹配/天梯，需升级为服务端模拟校验（物理内核是纯 TS，可直接跑在服务端——这是该架构的隐藏红利，两方案均保留此升级路径）。

### 4.2 开局协议

1. 房主创建房间 → 生成 `matchId` + `seed`（摆球种子）
2. 客队加入 → 双方 `createInitialWorld(seededPRNG(seed))` → 初始球堆一致
3. 先后手约定：host 开球（或随机，结果写入房间记录）

### 4.3 断线重连

- 所有 `ShotRecord` 持久化（追加式日志）
- 重连方拉取 `seq > 本地最大 seq` 的记录，**不重放动画**，直接取最后一条的快照恢复球位 + 规则状态
- 规则状态（`MatchState`）由最后一条记录 + 规则机重推，或随快照冗余存储一份

### 4.4 回合校验

- `seq` 严格递增 + `actor` 严格交替（连续进球不交替，由规则机决定）→ 服务端/接收方拒绝乱序与越权出杆
- 重复收到同 `seq` → 幂等丢弃

---

## 5. 方案 A：Kimi Websites 数据库轮询

### 5.1 平台能力边界（官方文档，2026-07-27）

已核实（kimi.com 帮助中心《Kimi Websites 网站生成功能介绍》《全栈能力说明》）：

- ✅ 全栈生成：前端 + 轻量后端 + 数据库持久化，可视化数据库面板
- ✅ 用户登录（Kimi 账号，或数据库账号密码）
- ✅ 一键发布 `*.ok.kimi.link`、版本管理、代码导出
- ❌ 官方明示限制：第三方支付、第三方 OAuth、复杂服务端 API 集成
- ⚠️ **文档未提及 WebSocket / SSE / 任何服务端推送能力**，其后端定位为"轻量后端逻辑 + 数据库读写"，按**仅支持 HTTP 请求/响应**评估

### 5.2 架构

```
玩家甲浏览器                          玩家乙浏览器
  ├─ 游戏（React+Three.js）             ├─ 游戏（React+Three.js）
  │    └─ net/ 轮询客户端                │    └─ net/ 轮询客户端
  └───────────┬──────────────────────────┘
              │ HTTPS REST（写击球 / 轮询新杆）
        Kimi Websites 托管后端
              ├─ 登录鉴权（Kimi 账号）
              └─ 数据库：rooms / matches / shots / results
```

**集成形态（推荐 iframe 嵌入，而非代码迁移）**：

- 游戏本体继续由妙搭托管（保留现有构建、分包、41 断言浏览器回归体系）
- Kimi 建站生成"大厅站"：登录、开房、邀请链接、战绩榜
- 对战页以 iframe 嵌入游戏，游戏内 `net/` 模块直连 Kimi 后端 REST API
- **绝不**把 6k+ 行确定性物理内核交给 AI 生成式沙盒重写——这是本方案的第一红线

备选形态：把 `dist/` 作为静态资源上传进 Kimi 项目。缺点是每次游戏更新要重新搬运，且 Kimi 沙盒对 Vite 分包产物的适配未经验证。

### 5.3 数据表设计

```
rooms    (id, code 邀请码, host_user_id, guest_user_id?, status, created_at)
matches  (id, room_id, seed, host_actor_first, status, winner?, created_at, ended_at?)
shots    (id, match_id, seq, actor, input_json, settled_json, created_at)
          UNIQUE(match_id, seq)   ← 幂等 + 排序锚点
results  (id, match_id, host_user_id, guest_user_id, winner, shots_count, duration_sec, created_at)
```

### 5.4 同步时序

```
出杆方                          服务端                       接收方
  │  POST /shots {record}        │                            │
  │─────────────────────────────>│                            │
  │                              │  INSERT（seq 唯一约束）     │
  │                              │<──── GET /shots?match_id&after_seq=N ────│  ← 每 1.5s 轮询
  │                              │────────────────────────────>│
  │                              │                       本地重放 + 快照校正 │
```

- 轮询节奏：对手回合 1.5s；自己回合降至 5s 心跳；可用 `If-None-Match`/seq 增量避免全量拉取
- 延迟构成：出杆到对手感知 = 上传（~0.2s）+ 轮询周期（平均 0.75s，最坏 1.5s）≈ **1–2 秒**，落在一杆动画时长内，可接受

### 5.5 方案 A 利弊

**优势**

1. **零运维**：不开服务器、不管进程，数据库面板可视化排查对局数据
2. **登录/身份白送**：Kimi 账号体系直接可用，邀请链接天然适配分享场景
3. **战绩/排行榜（R8）几乎零成本**：本就是数据库 CRUD 的主场
4. **上线速度快**：大厅站对话式生成，MVP 周期以天计
5. 适合**验证玩法**：先跑通"熟人约战"场景，验证对战乐趣再决定是否投入实时基础设施

**劣势**

1. **无实时推送**：R7（瞄准观战）永不可行；出杆感知有 1–2s 固定延迟；对手"是否在线/是否在思考"无 presence 能力（只能靠心跳表模拟）
2. **工程迁移风险**：Kimi 建站是 AI 生成式全栈沙盒，游戏本体无法整体托管（第一红线），形成"妙搭游戏 + Kimi 大厅"双站点架构，cookie/登录态跨域、iframe 通信等胶水问题需实测
3. **平台锁定**：官方明示云端数据库与 Kimi 登录**不随代码导出迁移**；未来迁走需重做登录与数据层
4. **容量不透明**：1.5s 轮询 × 每局约 20–40 杆 × 并发房间数，对"轻量后端"的实际承载无公开数据；高峰期被限流则体验崩塌，且无法自主扩容
5. **调试链路长**：后端代码由 AI 生成维护，出现并发/一致性 bug 时，靠对话迭代修复的可控性差

### 5.6 方案 A 风险清单

| 风险 | 等级 | 缓解 |
|---|---|---|
| 轮询被限流/性能不足 | 高 | MVP 期限制同时在线房间数；轮询间隔自适应 |
| iframe + 跨域鉴权不通 | 高 | 决策前先做 1 天 spike：Kimi 建一个最小后端 + 本地游戏调通读写 |
| 平台锁死 | 中 | 协议层（ShotRecord）与传输层解耦，迁移只换 net/ 实现 |
| 后端代码不可控 | 中 | 数据表与 API 契约写死在提示词与验收用例中 |

---

## 6. 方案 B：静态托管 + 专用实时层

### 6.1 架构

```
玩家甲浏览器                          玩家乙浏览器
  ├─ 游戏（现有代码 + src/net/ 新模块）  ├─ 游戏（现有代码 + src/net/ 新模块）
  └───────────┬──────────────────────────┘
              │ WebSocket（房间制消息路由）
        实时服务（房间 = 连接组 + ShotRecord 日志）
              └─ 持久化：自带存储 或 外接数据库
```

游戏本体**一行构建配置不改**，继续妙搭静态托管；新增一个 `src/net/` 同步模块（对应 AI Hook 的替换点，见 2.4）和一个独立部署的实时服务。

### 6.2 候选实时服务对比

| 候选 | 机制 | 免费额度 | 国内可达性 | 备注 |
|---|---|---|---|---|
| **Cloudflare Workers + Durable Objects** | 每房间一个 DO，WebSocket Hibernation 省费 | 每日 10 万请求 + DO 免费额度，MVP 够用 | ⚠️ 无 ICP 合作时国内访问不稳 | 生态最成熟，PartyKit 即基于它 |
| **PartyKit** | 房间即服务器，API 极简 | 有免费档 | ⚠️ 同上（CF 网络） | 开发体验最好，适合 1–2 天出原型 |
| **Supabase Realtime** | postgres_changes 广播 + presence | 免费档含 Realtime | ⚠️ 海外节点 | 数据库 + 推送一体，最接近"方案 A 体验 + 实时" |
| **国内云（腾讯云 IM / 声网 / LeanCloud / 阿里云 WebSocket 网关）** | 商用 IM/实时通道 | 多有免费档 | ✅ 稳定 | 引入重 SDK，与"小协议"不匹配，杀鸡用牛刀 |
| **自建 Node + ws（国内 VPS/轻量服务器）** | 100 行内的房间中继 | VPS 费用（约 ¥30–60/月） | ✅ 自己控制 | 协议最小、无锁定；要管进程与 HTTPS 证书 |

**用户在国内（妙搭托管、飞书生态）这一约束下**：CF/PartyKit/Supabase 的国内可达性必须实测后再选；若不可接受，自建 Node + ws 反而最省事——本协议（第 4 节）的服务端逻辑只是"房间路由 + 追加日志 + seq 校验"，核心代码量估计 100–200 行，且 `physics.ts` 为纯 TS，未来可直接在同一进程做服务端校验升级防作弊。

### 6.3 消息协议（WebSocket）

```
C→S  { t:'create', nickname }                 → S→C { t:'room', code, matchId, seed }
C→S  { t:'join', code, nickname }             → 双方  { t:'start', seed, first:'host' }
C→S  { t:'shot', record: ShotRecord }         → 对手  { t:'shot', record }   // 服务端校验 seq/actor 后转发+落库
C→S  { t:'aim', angle, power, spinX, spinY }  → 对手  { t:'aim', ... }       // R7 可选，10–15Hz 节流，不持久化
C→S  { t:'resume', matchId, lastSeq }         → S→C { t:'catchup', shots:[...] }
C→S  { t:'ping' }                             ↔ presence / 断线判定（30s 超时广播对手）
```

### 6.4 方案 B 利弊

**优势**

1. **真·实时**：出杆感知 < 300ms；presence（在线/思考中/掉线）原生支持；R7 瞄准观战可做
2. **游戏代码零侵入风险**：`src/net/` 为新增模块，物理/规则/渲染层不动，`npm run check` 全部门禁继续有效
3. **断线重连体验完整**：连接级状态 + 服务端日志，刷新页面秒恢复
4. **无平台锁定**：协议自定义，服务端可换实现；防作弊升级路径清晰（服务端跑同一套物理内核校验）
5. **可平滑叠加数据库**：战绩榜需要时再接任何数据库，不与实时层耦合

**劣势**

1. **多一个要运维的服务**：部署、监控、证书、进程守护（自建方案）；或受第三方平台配额/网络波动支配（CF 等方案）
2. **工程量大于 A**：net 模块 + 房间生命周期 + 重连 + presence，估计 3–7 天（A 的 MVP 约 1–3 天，但 A 有 spike 前置）
3. **登录/身份要自己做**：熟人约战可用"昵称 + 邀请码"绕过（推荐 MVP 做法）；正式账号体系是额外工作
4. **成本**：自建 VPS 有固定月费；CF 系免费额度够 MVP 但规模化后计费
5. 国内可达性若选自建以外方案，需先实测

### 6.5 方案 B 风险清单

| 风险 | 等级 | 缓解 |
|---|---|---|
| CF/Supabase 国内访问不稳 | 高 | 决策前实测；不达标则自建 Node+ws |
| 房间状态机 bug（并发加入、重复开球） | 中 | 服务端逻辑控制在 200 行内 + 单测 |
| 重连边界（停球瞬间掉线） | 中 | seq 增量拉取 + 快照直达（4.3 已覆盖） |
| 运维负担 | 低-中 | 初期手动运维；对局量小 |

---

## 7. 对比总表

| 维度 | 方案 A：Kimi 建站轮询 | 方案 B：实时层 |
|---|---|---|
| 出杆感知延迟 | 1–2 s（轮询周期） | < 300 ms |
| 瞄准观战 R7 | ❌ 不可行 | ✅ 可做（节流广播） |
| 在线状态/掉线检测 | 弱（心跳模拟） | 原生支持 |
| 登录身份 | ✅ 白送 | 需自建（MVP 可用昵称+邀请码） |
| 战绩排行榜 | ✅ 顺手 | 需另接数据库 |
| 游戏代码风险 | 双站点/iframe 胶水 | 仅新增 net/ 模块 |
| 运维成本 | 零 | 有（自建）或平台依赖（CF 系） |
| 平台锁定 | 高（数据与登录不可导出） | 无（自建）/ 低（CF 系） |
| MVP 工期估计 | 1–3 天（含 spike） | 3–7 天 |
| 长期演进（天梯/防作弊） | 受制于平台后端能力 | 路径清晰 |
| 国内用户可达性 | ✅ 好（Kimi 国内服务） | 自建 ✅ / CF 系 ⚠️ 待实测 |

## 8. 决策建议（供参考，非结论）

- **若目标是"最快验证对战玩法有没有意思"**（熟人约战、能接受 1–2s 延迟、不要瞄准观战）：选 **A**，但必须先做 0.5–1 天 spike 验证"妙搭游戏 iframe 嵌入 + 调通 Kimi 后端 REST 读写"这条链路，不通则直接转 B。
- **若目标是"正经的对战产品体验"或希望一次到位**：选 **B**。国内可达性实测若否决 CF 系，自建 Node + ws 是本协议下的最优解（服务端 200 行内、无锁定、防作弊升级路径最短）。
- **两方案不互斥**：第 4 节协议是共用的，传输层封装在 `net/` 接口后。可以 A 先跑 MVP，协议不变，日后把传输实现换成 WebSocket 即平滑升级到 B——**建议无论选哪条，都按"协议与传输解耦"来约束实现**。

## 9. 开放问题（请 codex 重点审核）

1. 快照权威模型下，`settled.facts` 由出杆方上报 vs 接收方从快照自行推导——后者更防篡改但要求两端规则/物理版本严格一致，如何取舍？版本号协商机制是否必要（net 握手时交换 build hash）？
2. 轮询方案下，"对手中途强退"的判定：心跳超时多少秒判负/判和？规则层是否需要新增"弃权"结局（`MatchState.winner` 目前只有正常胜负）？
3. `MatchState.actor` 双端各自映射 self→player 的同构假设，在观战（未来第三人在线看棋）场景是否成立？观战者视角应为只读快照流，协议是否现在预留 `role: 'spectator'`？
4. Kimi 建站后端生成代码的并发写入一致性（`UNIQUE(match_id, seq)` 冲突重试）能否在提示词层面可靠约束？spike 阶段如何验收？
5. 方案 B 自建服务端时，HTTPS/WSS 证书与妙搭静态站的跨域（CORS）配置是否存在未预见障碍？
6. 是否需要现在就约定 `ShotRecord` 的 schema 版本字段，以便后续加字段（如杆法扩展）不炸旧客户端？

---

## 附：关键源码事实索引（审核时可快速核对）

| 事实 | 位置 |
|---|---|
| 击球输入契约（4 浮点 + spin） | `src/physics.ts:257` `strikeCueBall` |
| 摆球 rng 可注入 | `src/hooks/useGameState.ts:29,58`；`physics.ts:160` 注释 |
| AI 执行误差用 Math.random（不进网络协议） | `src/physics.ts:363` |
| AI 回合编排（PvP 替换点） | `src/hooks/useOpponentAI.ts` |
| 规则状态机原子迁移 | `src/match/match-machine.ts`；契约类型 `src/match/types.ts:50` |
| 双角色 actor 定义 | `src/match/types.ts:8` |
| 固定步调度 | `src/simulation/fixed-step-runner.test.ts` 对应实现 |
