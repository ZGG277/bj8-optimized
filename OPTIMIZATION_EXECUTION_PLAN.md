# 球桌、相机与物理优化执行方案

日期：2026-09-05。负责人：主 Session（方案、调度、整合、验收与交付）。

最终状态：本轮本地实现与重点验收完成，46文件/329项测试、类型检查、构建通过；正式构建已在1280×800与390×844实际浏览器入口验证。未提交或发布；真机触控/长期温控与实物标定留作明确后续，完整证据见 `OPTIMIZATION_ACCEPTANCE.md`。

## 0. 目标、范围与交付边界

本轮完成可运行的本地优化，保留现有按钮引导、默认白球、瞄准线/拨轮以及用户的开局页改动。目标按顺序为：

1. 瞄准时目标球与目标袋口完整可见，在稍高于第一人称的偏好下尽量放大主体。
2. 击球后和对手回合完整显示六袋/球台，横屏横放、竖屏纵放，减少无效留白。
3. 修复高速擦球漏碰，改善中心击球、旋转传递与碰库，保证物理、辅助线、规划使用一致模型。
4. 袋口采用连续的实体结构与有厚度网袋，落袋保持球的真实尺寸，表现入射速度和旋转差异。

交付物：分层执行文档、实现源码、针对性回归、可复现的浏览器证据、最终验收记录。当前授权为文档、本地实现、子 Agent 调度和本地验收；提交、推送、飞书文档回写、妙搭发布不在本轮执行范围内。

真实球台的材质参数不能从代码推导：本轮建立物理合理的模型及可重复的标定入口；没有实测数据时，不把任何默认参数称为“与真实乔氏球台标定一致”。完整跳球、弧线球、软体绳网不作为本轮目标。

## 1. 基线与保护

- 唯一产品源码：`bj8-main-integration/`，`main`，HEAD `0e1e7193971d0aeea78075c4d980058ea25e63e2`，版本 1.4.1。
- 文档中的 `guagua-pool-share` 是冻结分享快照，不接收本轮开发。
- 工作树已有上一轮按钮引导/自动切镜未提交实现，属于本轮输入，不能覆盖或重置。
- 用户开局页文件只读保护：`src/components/IntroScreen.tsx`（SHA-256 `352dc8bd4ad1f5d7fd0ca16bc0829101be27c0ec7973b1ee4957fd98cbb1cb1c`）、`src/styles/layout.css`（`69accbe0b39fc3eb980ba73d43e8181e47f232e98df7d98287f6714ffdac3953`）。
- `npm ls --depth=0` 本轮成功；只使用 npm，无需安装依赖。不得改锁文件。
- 全部 Agent 共享源码工作树，按文件所有权协作；禁止回退他人改动。
- 并行编写可以，完整构建、重物理基准和 3D 浏览器验收串行；只启动一个本任务 Vite 服务。

## 2. 审核证据与设计决策

### 2.1 相机

现有 `cameraPoseAt` 缺少目标球、袋口与 HUD 安全区输入；幽灵球确认仅切到固定 16% 高度。已数值复现 390×844 薄切局面目标袋口越出右边界。

采用“完整可见”为硬约束，“低机位”和“主体大”为软目标的构图器：

- 输入：白球/幽灵球/首碰目标球/候选袋口边缘、宽高比、安全区、用户当前杆向。
- 输出：唯一 camera pose、是否回退及构图依据；不能改变物理瞄准角。
- 稳定候选袋口：沿预计目标球出射方向选择，不能替用户偷偷瞄准；候选不可靠时使用宽构图，不频繁跳袋。
- 拟合球完整轮廓和袋口开口，不能只检查球心；机位保持在台面之上，必要时小幅升高或后退。
- 全台拟合采用完整六袋与木帮必要边界，按实际视口求最近安全机位，取消盲目的固定最小高度与额外留白。
- 拾取相机与显示相机共享同一计算路径；拖动中冻结构图输入，抬手后再切换，避免反馈跳动。
- 真实击球接触后回全台；取消蓄力/摆实体白球不切到瞄准位。对方回合保持全台。

### 2.2 袋口渲染

现有台呢、袋腔、皮革和暗口由局部不同曲线生成；网袋为单像素线；入袋后固定 0.45 秒、固定 6cm 位移并缩球。

- 统一袋口局部剖面与几何来源，明确物理球心安全线和真实可见孔洞边不是同一个轮廓。
- 保留现有六袋索引和尺寸，不通过放大洞口掩盖碰撞缺陷。
- 合并低面数绳索网格，远景简化，避免大量独立 draw calls。
- 下落球不缩放；依据 entry position/velocity 建立确定性下落轨迹和遮挡，禁止沿固定向量穿过袋壁。
- 视觉先改善剖面、倒角、接触暗部，再做皮革粗糙度；保持低功耗/按需渲染/温控策略。

### 2.3 物理

已复现：两球轨迹间距 56mm，小于球径 57.15mm，10m/s、1/240秒步长下步末没有重叠，`events=[]`。现有 TOI 只在步末重叠后回滚，不能覆盖整个时间区间。

- 在固定步内按最早接触推进，球球/库边/袋口使用一致时间序列；保留可重复与有界求解预算。
- 水平中心击球初始以滑动为主；高低杆/侧旋由接触偏移与冲量生成。
- 球球摩擦使用接触点相对速度，同时更新平动和旋转；辅助预测与真实碰撞共享公式。
- 同时多接触保留稳定处理，防球堆索引顺序影响、能量注入及静止抖动。
- 库边将法向恢复和切向摩擦/旋转耦合，用能量、反射和转移方向基准检查；速度增长须由旋转能转移解释。
- 袋口局部物理/真实参数标定分开：先避免错误提前吞球与事件顺序错误；更深的支撑/弹出模型只能在保持规则/规划一致的前提下接入。

## 3. 分层任务树（WBS）

### 0 项目控制 — 主 Session

- 0.1 核对基线、依赖、已有改动和文件保护哈希。
- 0.2 固化本计划、任务合同、模型路由与状态。
- 0.3 处理跨模块接口和变更冲突；任何 Agent 不得越权改其他所有者文件。
- 0.4 每个交付门禁通过后更新状态与证据；失败回到明确责任任务。

### 1 自适应相机 — camera Agent

- 1.1 纯构图类型及拟合算法：完整球体/袋口投影、视口安全边距、最近可行机位。
- 1.2 全台构图：桌面横向/手机纵向、六袋与必要木帮不裁切、最大占屏。
- 1.3 瞄准语义：识别首碰目标与稳定袋口，未知目标回退，不改瞄准角。
- 1.4 对局接入：幽灵球抬手确认进入自动构图，真实击球/对手回合回全台，保留手动视角。
- 1.5 纯几何回归：长台、薄切、近袋、贴库、无目标、横竖视口与边界尺寸。
- 1.6 提供 Scene3D 接入合同；渲染 Agent 释放文件后，由 camera Agent 仅接管相机段落，主 Session 核对活相机/虚拟拾取相机一致性。

### 2 袋口渲染与下落 — pocket Agent

- 2.1 抽离袋口渲染责任，统一局部剖面/皮裙/袋腔接缝。
- 2.2 以合并低面数绳索替代无厚度网线，保持远景预算。
- 2.3 下落纯轨迹：保持球尺寸，消费入射速度，约束在袋腔内，旋转连贯。
- 2.4 Scene3D 接入与资源释放：几何/材质共用、dispose 无泄漏、按需渲染。
- 2.5 数学/结构回归及浏览器需要观察的具名对象和截图机位交接。

### 3 物理正确性 — physics Agent

- 3.1 全时间区间球球 CCD 与高速薄擦复现回归。
- 3.2 球球、库边、落袋的时间排序和多接触稳定性。
- 3.3 击球点到初始旋转的合理映射，滑动—滚动转换不失真。
- 3.4 球球切向摩擦和旋转耦合，共享预测/反解公式。
- 3.5 库边耦合与能量护栏；保持定杆、跟进、低杆、加塞有可解释的区别。
- 3.6 检查规划、对手、复盘依赖并更新必需的测试真值，不得简单降低阈值让测试变绿。
- 3.7 给出基准参数、未实测项、性能与局部袋口剩余限制。

### 4 集成与回归定义 — 主 Session

- 4.1 合并相机构图与 Scene3D，统一拾取/渲染、投影安全区与控件布局。
- 4.2 审核 physics 对 aim/planner/Scene3D 预测的接口一致性。
- 4.3 定义固定回归命令和预期，锁定源文件指纹后派 Spark；有新改动则旧结果标 STALE。
- 4.4 编写/适配真实浏览器的相机、袋口、触控和新手提示关键旅程，不通过直接改 React 状态假装操作成功。
- 4.5 更新 L3 → L2 → L1 文档及 README，把暂未证实项如实保留。

### 5 验收与交付 — Spark + 主 Session

- 5.1 Spark 固定机器回归：typecheck、冻结的 Vitest 清单、完整 check；只运行和报告，不改代码。
- 5.2 主 Session 真实入口验收：1280×800、390×844，以及横竖旋转；独立体验 Agent 如派发则由其执行完整旅程，主 Session 不重复整套。
- 5.3 六袋远/近视角与慢/快/擦角下落截图；验证尺寸、接缝、遮挡及页面错误。
- 5.4 最终 diff、保护文件哈希、构建 SHA-256、进程清理与授权边界检查。
- 5.5 输出执行结果与明确未通过项；测试绿不等于真机通过，完成本地不等于发布。

## 4. 模型与文件所有权

| 角色 | 模型/能力 | 写入范围 | 禁止事项 |
| --- | --- | --- | --- |
| 主 Session | 当前高能力模型；方案、整合和开放式诊断 | 执行文档、根/src 地图、README、脚本；Scene3D 相机接入须等待 pocket 交付 | 不覆盖已有用户文件、不发布 |
| camera | gpt-5.6-terra / high | camera-view、新增 camera-framing、Game 相机编排、相关新增 hook/测试；pocket 释放后仅接管 Scene3D 相机段落 | 不编辑 Scene3D 袋口段落/physics/用户开局页 |
| pocket | gpt-5.6-terra / high | Scene3D 袋口段落（首轮已释放）、独立 pocket-render/pocket-drop 模块与测试/地图 | 返工阶段不再编辑 Scene3D；不编辑 Game/camera-view/physics 几何规则 |
| physics | gpt-5.6-sol / high | physics.ts、physics/、aim/、相关物理/规划依赖测试及地图 | 不编辑 Game/Scene3D/camera 模块，不安装依赖 |
| regression | gpt-5.3-codex-spark（可用性以实际执行为准） | 仅约定的验收日志/结果；源码只读 | 不诊断式改代码、不改测试、不启动重复服务 |

Spark 在当前协作工具的模型清单中未列出，先通过官方 Codex CLI 显式选用并执行固定合同；如模型不可用或认证/环境失败，记录原因并由主 Session 执行相同冻结清单，不冒称 Spark 已完成。所有 Agent 的完成声明均须主 Session 核对。

## 5. 依赖、节奏与停止条件

第一批：1 相机、2 袋口、3 物理并行编写，各自只跑便宜的针对性检查。重仿真由主 Session 排队。

第二批：三个实现交付 → 4 接口整合及定向检查 → 串行真实浏览器验收 → 必要修复及受影响重验。

第三批：页面验收后的最终源码冻结 → Spark 固定完整回归与构建 → 核对源码未变化/构建指纹 → 文档/证据/进程收口。此顺序在首轮模块交付后调整，避免页面返工导致重复完整构建；浏览器截图绑定开发源码指纹，构建产物另做入口冒烟。

停止并上报的情况：同一文件出现非本任务并行修改且无法安全合并；模型/沙箱权限无法执行必需操作；真实物理标定缺少测量数据；继续必须发布或扩大外部影响。一般代码失败在授权范围内修复，不把测试失败直接甩给用户。

每个 Agent 至少在首次方案、首个可用实现、检查完成时报告；超过合理执行窗口无新证据时主 Session 查询状态或收回任务。

## 6. 验收标准

| 编号 | 通过标准 | 证据 |
| --- | --- | --- |
| C1 | 幽灵球确认后白球/目标球/目标袋口完整轮廓在安全可视区 | 投影断言 + 电脑/手机截图 |
| C2 | 全台横/竖排列正确、六袋完整，机位接近安全拟合最优值 | 几何拟合误差与浏览器边界 |
| C3 | 取消、实体摆球不误切镜；出杆接触后与对方回合全台 | 真实 Pointer 旅程 |
| C4 | 相机移动不污染杆向；拨轮和手动视角可用 | 角度前后记录与真实触控 |
| R1 | 六袋接缝连续，皮裙/袋腔/台呢无明显穿插 | 固定近景/俯视截图 |
| R2 | 下落全过程 scale=1，速度影响轨迹，球不穿皮圈/袋壁 | 轨迹断言 + 动态采样 |
| R3 | 静止停止空转，无明显新增 draw-call 爆炸或资源泄漏 | renderer 指标/既有温控门禁 |
| P1 | 56mm/10m/s 薄擦不漏碰，球序交换结果等价 | 确定性物理回归 |
| P2 | 事件按步内时间一致推进，无重复首碰/落袋，密集开球有界 | 事件序列/球堆回归 |
| P3 | 中杆初始滑动，高低杆和侧旋按物理方向传递，能量不凭空增加 | 解析/数值基准 |
| P4 | 辅助预测与实际出球方向一致，规划/对手/规则无破坏 | 现有专项与完整单测 |
| G1 | 既有按钮提示及开局用户改动保留 | 保护哈希 + 关键入口冒烟 |
| G2 | typecheck、单测、构建通过，入口实测无页面错误 | Spark/主 Session 日志与构建哈希 |

测试不能只照抄实现公式：CCD 使用独立几何最短距离真值，物理使用动量/能量/对称性基准，相机用完整投影轮廓与安全空间，视觉用真实页面截图。

## 7. 进度与证据

| 工作包 | 状态 | 负责人 | 当前证据/下一步 |
| --- | --- | --- | --- |
| 0 基线/合同 | 完成 | 主 Session | HEAD/依赖/已有改动已核对，本计划已创建 |
| 1 自适应相机 | 已实现，页面重点旅程通过 | camera_optimization / Terra high | 44 项定向测试/typecheck；修复投影镜像、薄切反向选袋、默认拨轮遮挡、非对称安全区引起的全台偏航；手机纵台及真实拖放/拨轮、桌面出杆/取消/对手旅程已验证 |
| 2 袋口渲染 | 已实现并完成重点近景验收 | pocket_optimization / Terra high + 主 Session | 袋口19项与新增斜接1项通过；六袋法线无翻转，角袋/中袋完整reload复验通过；慢/快/中袋各86次浏览器采样全部scale=1并实际隐藏；物理开口未扩大 |
| 3 碰撞物理 | 完成 | physics_optimization / Sol high | CCD/接触岛/旋转通过；下游两失败已分类为强低杆整台回拉洗袋与旧强制低杆真值，保留强低杆风险用例及安全MC≥.9门槛，未改产品迎合测试 |
| 4 集成 | 完成 | 主 Session | 活/虚拟相机同源、DOM安全区和真实触发语义验证；接缝补修后完整reload近景通过；文档地图/保护哈希已核对 |
| 5 固定回归/体验验收 | 本地重点验收完成 | Spark首轮 / 主 Session最终 | 首轮Spark 326/328并发现2问题；Sol定向核实后主Session最终46文件/329项全过、typecheck/build通过；构建HTML与预览响应同哈希，桌面/手机视口真入口通过；测试服务及标签已关闭 |

## 8. 参考依据

- 本地 `ONBOARDING_CAMERA_ACCEPTANCE.md`：上一轮改动与浏览器阻塞检查点。
- `docs/REPOSITORY-CONSOLIDATION.md`：唯一源码/分享快照/发布包装边界。
- Three.js 普通线宽限制：https://threejs.org/docs/pages/LineBasicMaterial.html
- 自然滚动与击球偏移：https://drdavepoolinfo.com/faq/follow/roll-and-overspin/
- Mathavan 库边动力学及适用范围：https://drdavepoolinfo.com/physics_articles/Mathavan_IMechE_2010.pdf
- pooltool 模块化物理模型：https://pooltool.readthedocs.io/en/latest/resources/custom_physics.html

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md

## 9. 固定回归执行合同（源码冻结后启用）

### 9.1 Spark 机器回归

输入由主 Session 填入：`node scripts/optimization-snapshot.mjs` 的 `head/sourceSha256`。测试前后必须一致；文件修改期间不能运行本合同。

精确步骤：

1. `node scripts/optimization-snapshot.mjs`，核对 HEAD、源码指纹与保护文件哈希。
2. `git diff --check`。
3. `npm run check`（typecheck → 全部 Vitest → build，内部串行，任一步失败停止）。
4. 再运行 `node scripts/optimization-snapshot.mjs`，确认源码指纹一致并记录本次构建 SHA-256/bytes。
5. 报告各命令退出码、测试文件/断言数量、失败摘要、最终源码/构建指纹。禁止修改源码、测试或锁文件；失败由主 Session 诊断。只可产生测试工具缓存和 `dist/` 构建输出。

有效性：模型不可用标记 `MODEL_UNAVAILABLE`，输入变化标记 `STALE`，任一命令失败标记 `FAIL`；均不可写为 PASS。

### 9.2 浏览器真实旅程

固定入口为本工作树 `http://127.0.0.1:5199/`。使用新建、可丢弃的专用浏览器空间；不清理用户真实浏览器存储。开发调试 API 只允许构造球局和观测状态，关键瞄准/出杆/取消必须使用真实鼠标或 touch 事件。

桌面 1280×800：

1. 经介绍页实际按钮进入陪练；确认默认白球/瞄准线/拨轮，拍全台横向截图。
2. 悬停未使用按钮，确认提示位于左侧或安全边缘回退；成功操作后再次悬停与刷新验证该项不再提示。
3. 用固定球局（白球 `(0,0.9)`、目标球 `(-0.4,-0.8)`、其余球暂不参与）测试向右上袋的薄切构图；用真实台面指针确认幽灵球，读取目标球与袋口完整投影，拍瞄准截图。
4. 从出杆控件真实蓄力后移出取消：shot 计数不变、保留瞄准构图；再正常击球：shot 增一，实际 contact 后回全台。
5. 六袋结构读取与固定角袋/中袋近景，确认球网实体、护口连续；慢/快落袋采样球 mesh.scale 与轨迹，截图记录。

手机 390×844（touch）：

1. 新局确认六袋纵向全台，完整木帮/台面不被裁切。
2. 对固定薄切球局重复真实点按/拖放与出杆，确认完整入镜及无首触卡顿。
3. 拨轮慢速移动超过长按阈值仍调杆向、不误拖布局；取消不学习，触屏不产生粘滞悬停。
4. 将对手回合构造为暂停可观测状态，确认观战全台且不能误调杆向；交棒保留全台，下一次幽灵球落位恢复瞄准构图。
5. 改为 844×390，观察横台重排后六袋仍完整，再恢复竖屏。

截图必须标识当前构建/源码指纹和视口；历史截图不得替代本次证据。浏览器发现错误时先固定复现，不以多次刷新碰运气；代码修复后只补受影响旅程。

## 10. 后续实测标定包（不冒充本轮已完成）

本轮解决模型/数值缺陷，并保留米、秒、弧度单位；真实手感仍需指定球台的测量数据。后续可按以下可独立验收的子包推进，而不是盲调一组常数：

1. **10.1 布面基准**：同一球台、球组与环境，记录中心杆在多档初速下的滑动到滚动时间、全程距离与停止时间；至少每档 5 次。分开拟合滑动摩擦、滚动阻力、侧旋衰减，不能用一个减速系数包办。
2. **10.2 球球接触**：正碰与多个切角，记录入射速度/杆法、碰后双方方向和速度；检查线动量、总能量与侧旋传递方向。当前共享方向预测是接触瞬间近似，不代表完整高低杆终态走位。
3. **10.3 库边参数**：多档法向速度、入射角及正/反塞，拟合恢复与切向摩擦；标注库鼻高度、胶条和台呢，不能直接照搬另一款球台论文的系数。
4. **10.4 袋口局部支撑模型**：用慢滚挂袋、擦角、快球吐袋视频，联合约束可见孔洞、库鼻碰撞和失去支撑时刻。需要引入局部球高/垂直速度时，必须同步 physics/aim/planner/Scene3D；现有二维捕获弧不能称为完整袋口刚体模型。
5. **10.5 标定验收**：留出至少 30% 未参与拟合的局面，比较轨迹位置、速度、方向、落袋/未落袋分类与误差分布；报告设备帧率和像素到米的测量误差。只有误差门限事先约定并在留出集通过后，才称“针对该球台完成标定”。

推荐采样字段：`tableId, clothId, ballSetId, date, temperature, humidity, shotId, cueOffset, initialSpeed, initialSpin, contactAngle, outgoingSpeed, outgoingAngle, travelDistance, stopTime, pocketResult, videoFrameRate, measurementUncertainty`。缺少高帧率或角速度测量时允许记录缺失值，禁止反推一个看似精确的实测量。
