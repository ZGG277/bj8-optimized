# 按钮提示与自动视角：本地验收检查点

日期：2026-09-04（Asia/Shanghai）。状态：本地代码与构建检查通过，真实浏览器验收尚未执行，未提交/推送/发布。

历史检查点说明：2026-09-05 已继续实现并完成部分原待验浏览器旅程，最新源码、构建及准确未验范围以 [OPTIMIZATION_ACCEPTANCE.md](OPTIMIZATION_ACCEPTANCE.md) 为准。下文保留当时事实，不代表当前仍被同一浏览器权限问题阻塞。

## 来源与范围

- 唯一源码工作树：`bj8-main-integration`，分支 `main`，基线 `0e1e7193971d0aeea78075c4d980058ea25e63e2`（v1.4.1）。本次实现仍是未提交改动。
- `IntroScreen.tsx` 与 `styles/layout.css` 是任务开始前已有的用户改动，本次未编辑，构建自然包含这两份当前本地文件。包装工作树未动。
- 单页构建：`dist/index.html`，1,266,742 bytes。
- 构建 SHA-256：`e77d218e4658a238eb502db5db01a4c99b55fb002a9357c8907d0eb1fa52d5aa`。

## 已验证

- `npm ls --depth=0` 成功，未安装或重写依赖。
- `npm run check`：TypeScript 通过；38 个 Vitest 文件、275 项测试全部通过；Vite 与单 HTML 构建通过。
- `git diff --check` 通过。
- 单测覆盖：每个控件独立持久化、受限存储降级、无变化/取消/禁用不学习；真实控件处理器成功事实；左侧优先定位及贴边上方避让；稍高出杆位；横/竖全台几何；幽灵球抬手确认、取消/桌外点/摆实体白球不误切镜。

## 浏览器环境阻塞

- Vite 默认沙箱启动失败：`listen EPERM`。
- 仅绑定 `127.0.0.1:5199` 的提权启动请求及一次允许的重试，均因自动权限审查超时未启动，不是代码运行失败。
- ego-browser 无法在默认沙箱连接 `ego_cli bootstrap`，工具报告需要沙箱外执行。
- 未打开测试页面，未建立浏览器任务空间；没有本任务遗留的 5199 监听服务。无本次真实截图或线上验收证据。
- 需获准本次本机服务/浏览器的沙箱外执行后再继续，不重复发布或安装环境。

## 待验收（不能以单测代替）

入口：届时从本工作树启动的 `http://127.0.0.1:5199/`。真实 Chromium 浏览器，无账号登录需求，使用可丢弃测试上下文；不修改用户浏览器进度。

1. 1280×800 电脑：开局白球合法就绪、默认瞄准线和拨轮可见，六袋完整且球台横放。
2. 鼠标悬停提示在按钮左侧；贴左屏边的入口上方避让；有效操作后该按钮提示立即消失，刷新后仍消失，未使用按钮继续提示。键盘聚焦可读；无效/取消动作不学习。
3. 台面点击或拖动瞄准幽灵球：按下/拖动中不切镜，抬手确认后 `data-view-level=0.160`，沿杆向略高于第一人称；重新摆实体白球或取消操作不误切镜。
4. 蓄力取消不切镜；真实击球后 `data-view-level=1.000` 并恢复全台。手动相机仍可使用，下一次幽灵球落位/击球恢复自动流程。
5. 390×844 手机：球台纵放且六袋完整；真实触控重复上述瞄准/出杆旅程，不卡首触、不出现粘滞悬停提示；横竖屏切换同步适配全台。
6. 记录最终源码/构建指纹、截图、关键旅程结果，结束后关闭本任务测试空间与服务；未经另行授权不提交、推送或发布。
