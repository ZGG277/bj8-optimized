# 三控件悬停说明专项

## 合同与范围

- 用户请求：单独派 Agent，为右侧视角轨道上方两个小按钮和下拉蓄力出杆控件补充功能解释。
- 执行：`control_hover_guidance`，GPT-5.6 Terra / high；中央负责源码边界与真实入口验收。
- 仅本地实现与验证，不提交、不推送、不发布、不写飞书。保留已有未提交改动和用户预览。
- 基线：main / `0e1e7193971d0aeea78075c4d980058ea25e63e2`；修改前源码指纹 `ff22c0458c0073b3b2c7bcf5417b3b9e5d00a3b1d6807a5dd81ee381765a50a8`。
- 保护文件：`IntroScreen.tsx`、`layout.css` 不修改；不修改相机、物理或袋口几何。
- 高能力袋口研究任务已暂停到本专项重任务结束，避免并行 3D 验收。

## 交互约定

仅俯视、手动视角、出杆三控件在已学会后仍显示简短悬停说明；保留原学习记录，不清空 localStorage。其他控件的一次性引导规则保持。当前采用已告知用户的推荐默认方案。

提示优先在控件左侧，不能拦截鼠标或蓄力拖动。键盘聚焦有同等说明，触屏不产生黏住的悬停提示。手动视角说明应跟随开关状态；出杆说明应匹配纵向下拉/横向右拉，并解释松开出杆和横向移出取消。禁用状态只允许只读说明，不得解禁或记为成功。

## 验收清单

1. 定向测试与 typecheck；交付前串行完整 check，记录实际结果。
2. 当前源码、dist 与 `http://localhost:5199/` 实际响应指纹对齐；预览服务不另开重复实例。
3. 桌面真实页面：三控件悬停均有对应说明、位置不遮挡控件、移出收起；已学会状态仍可查看。
4. 手动开关前后说明匹配；出杆提示不改变现有蓄力/取消语义。
5. 手机 390×844 检查控件与提示定位边界；未执行的触控旅程明确标为未验。
6. 保护文件哈希保持；独立验收标签关闭，用户预览保留。

## 验收结果（2026-09-05）

已完成本地专项实现与验收，未发布。中央复核补正两点：动态提示必须订阅 DOM 文案变化，不能仅等坐标变化重绘；退出手动视角实际保留当前观察角度，不应声称立即恢复自动跟随。

- 执行 Agent 定向测试最后一轮 30/30，通过 typecheck；中央校正退出文案后完整 `npm run check` 通过：46 文件、334 测试，typecheck 与生产构建成功。日志：`shots/control-hover-check.log`。
- 最终源码指纹：`f95ea7349ec417aecadbd40178d4897e1be42c330d3bc776138bb412bb4582eb`，149 文件；HEAD 未改变。
- `dist/index.html`：1,286,748 bytes，SHA256 `0a9ea5aedc4aa8bb3ad27f2d180a5993751bd33ee9e423db15021627c92bdee1`；实际 `http://localhost:5199/` 返回 200，正文逐字节匹配 dist。该入口为生产 preview，不是 HMR；本轮已真实重新构建。
- 保护文件保持：IntroScreen SHA256 `352dc8bd4ad1f5d7fd0ca16bc0829101be27c0ec7973b1ee4957fd98cbb1cb1c`；layout.css SHA256 `69accbe0b39fc3eb980ba73d43e8181e47f232e98df7d98287f6714ffdac3953`。

真实浏览器：ego-browser / Chromium，独立任务空间 13，本地应用无需登录；没有清空用户 localStorage。该空间已有包含 shoot、view-overhead、view-manual 的学习记录，三个控件依然真实鼠标悬停可见。

| 实际旅程 | 观察结果 |
| --- | --- |
| 1280×800 开始陪练、悬停俯视 | 显示「俯视：俯视全台」，左侧提示 |
| 悬停手动按钮、开启再退出 | 开启/退出文案与按钮状态即时对应，退出说明为保留当前观察角度；第二次切换未留旧文案 |
| 悬停纵向出杆 | 显示向下蓄力、松开出杆、横向移出取消、Enter 轻杆；提示 x=980..1208，控件 x=1218..1272，保持 10px 间隙，pointer-events:none，悬停不蓄力 |
| 移出、键盘焦点、Escape | 移出后 tooltip=0；键盘焦点显示同一说明及 aria-describedby；Escape 后 tooltip=0 |
| 390×844 竖屏三控件 | 三项说明可见；出杆提示 x=90..318，控件 x=328..382，未出屏、未遮挡控件 |
| 触摸屏幕、移出取消 | 真实 CDP touch 后 tooltip=0；真实拖动出杆控件到走廊外后 charging=false，仍为开球可用状态，未击球 |

截图已由中央打开自验：`shots/control-hover-desktop.png`、`shots/control-hover-mobile.png`。桌面 CSS 视口 1280×800，截图像素 2560×1600；手机截图 390×844。竖屏截图延续桌面退出后的保留视角，仅证明本次控件提示边界，不是手机默认全台构图的验收证据。

未验部分：横向停靠的方向文案有单测但未在真实页面重新停靠；对手回合禁用状态的真实浏览器悬停未走完整回合；真实手机硬件触控未验（本次为 Chromium 390×844 加 CDP touch）。未将这些范围声称为全量通过。

独立验收标签与空间在完成后关闭；保留原 5199 预览供用户刷新验收。本专项未修改袋口、物理和相机算法；袋口独立研究随后恢复。
